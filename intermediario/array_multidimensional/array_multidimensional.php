<?php 

	$multidimensional_arr = array(array('Nome1', 'Nome2'), array('Idade1', 'Idade2'));

	echo $multidimensional_arr[0][1];

	/*Array multidimensional nada mais é do que um array dentro de um array, e para chama-los é simples, passamos no echo duas chaves, onde a primeira correponde ao array em questão ou seja em qual chave do array principal ele está e depois em qual chave os valores desse array que foi pegado está*/

 ?>